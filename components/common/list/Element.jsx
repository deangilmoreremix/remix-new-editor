import React from 'react';

const Item = ({ handleSelect, item }) => {
  // console.log("handleSelect", handleSelect);
  console.log("item", item);
  return (
    <div className="list-item">

    </div>
  );
};

export default Item;
