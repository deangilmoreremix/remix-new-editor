# Local environment overrides — git-ignored
# SECURITY: Rotate any keys that were previously committed. Use deployment secrets.

# Backend
# MUAPI_API_KEY=set-via-deployment-secrets

# Frontend (Vite exposes VITE_-prefixed vars to the browser)
VITE_BACKEND_URL=http://localhost:3001

# Supabase (Lead Finder)
# SUPABASE_URL=set-via-deployment-secrets
# SUPABASE_SERVICE_KEY=set-via-deployment-secrets
