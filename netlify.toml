# Netlify build configuration
[build]
  command = "npm ci --include=optional --legacy-peer-deps && npm run build"
  publish = "dist"

[build.environment]
  NPM_FLAGS = "--include=optional --legacy-peer-deps"
  NODE_VERSION = "22"

# Model catalog alias
[[redirects]]
  from = "/api/model-catalog"
  to = "/api/model-catalog.json"
  status = 200
  force = true

# Stripe functions (friendly URLs)
[[redirects]]
  from = "/api/stripe-webhook"
  to = "/.netlify/functions/stripe-webhook"
  status = 200

[[redirects]]
  from = "/api/stripe-checkout"
  to = "/.netlify/functions/stripe-checkout"
  status = 200

[[redirects]]
  from = "/api/stripe-products"
  to = "/.netlify/functions/stripe-products"
  status = 200

# Subscription check (gating)
[[redirects]]
  from = "/api/check-subscription"
  to = "/.netlify/functions/check-subscription"
  status = 200

# Security headers
[[headers]]
  for = "/*"
  [headers.values]
    X-Frame-Options = "DENY"
    X-Content-Type-Options = "nosniff"
    X-XSS-Protection = "1; mode=block"
    Referrer-Policy = "strict-origin-when-cross-origin"
    Permissions-Policy = "camera=(), microphone=()"

# Ensure the SPA fallback only fires when no file matches.
# Netlify's default redirect behavior already skips existing files.
# We list the auth routes explicitly to eliminate any ambiguity
# about which paths need the SPA shell versus raw asset serving.
[[redirects]]
  from = "/signin"
  to = "/index.html"
  status = 200
  force = false

[[redirects]]
  from = "/signup"
  to = "/index.html"
  status = 200
  force = false

[[redirects]]
  from = "/forgot-password"
  to = "/index.html"
  status = 200
  force = false

[[redirects]]
  from = "/reset-password"
  to = "/index.html"
  status = 200
  force = false

[[redirects]]
  from = "/account"
  to = "/index.html"
  status = 200
  force = false

[[redirects]]
from = "/profile"
to = "/index.html"
status = 200
force = false

# SPA fallback: any path that doesn't match a real file or one of the
# explicit redirects above is served by the SPA shell (`/index.html`).
# This is the standard pattern for client-side hash-routed SPAs and
# prevents 404s for any menu link that incorrectly uses a real
# `<a href="/route">` instead of the in-app hash router.
# `force = false` so existing files (assets/, favicon, manifest, etc.)
# are still served directly without a redirect.

# OpenThorn studio: serve static files from /openthorn/ without SPA fallback.
[[redirects]]
  from = "/openthorn/:splat"
  to = "/openthorn/:splat"
  status = 200
  force = false

[[redirects]]
from = "/*"
to = "/index.html"
status = 200
force = false
