@pqina:registry=https://npm.pqina.nl/
//npm.pqina.nl/:_authToken=3CHFQxE4tyVODWDjm7.fMuliVcNpih9GXdlq7bQS.d88fsv/QCHqK

# Always install optional dependencies. This is required so that
# @tailwindcss/oxide's platform-specific native binary is present
# (see npm/cli#4828). Without this, Tailwind fails to load in CI.
include=optional