# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
.env

# Netlify auto-generated state (contains absolute local paths)
.netlify/

# .npmrc contains a private registry auth token and is already tracked
# in git history. Adding it here prevents future re-commits of the token.
# The include=optional setting needed for builds is in netlify.toml instead.
.npmrc

# Audit / test scaffolding — contains the sandboxed muapi key; never commit.
.audit/
muapi-audit-results.json

# Clerk auth test/debug scaffolding — never commit.
.agents/
clerk_e2e_test.cjs
debug_signin.cjs
skills-lock.json

