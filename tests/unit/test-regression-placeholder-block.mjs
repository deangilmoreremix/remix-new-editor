/**
 * Regression test: verifies client and proxy block static/demo outputs.
 *
 * Run:
 *   # Unit only (no network):
 *   node tests/unit/test-output-integrity.mjs
 *
 *   # Live proxy behavior:
 *   node tests/unit/test-live-proxy-outputs.mjs
 *
 * Acceptance criteria:
 *   - helper tests: all PASS
 *   - live proxy test: NO static/demo outputs, unique URLs per prompt
 */

export {};
