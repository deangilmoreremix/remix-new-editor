# FAQ Page Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Add a public `/faq` route with an accordion-style FAQ page covering Getting Started, BYOK & API Keys, Security & Privacy, and Pricing.

**Architecture:** A single `FaqPage` component holds all FAQ content as a typed data array. Each category renders a list of `AccordionItem` sub-components that toggle open/closed via local React state. The footer's "Docs & FAQs" link is wired to `/faq` via React Router.

**Tech Stack:** React 18, TypeScript, CSS Modules, React Router v6, `usePageTitle` hook (existing at `src/lib/usePageTitle.ts`)

---

### Task 1: Create FaqPage CSS module

**Files:**
- Create: `src/pages/FaqPage.module.css`

- [ ] **Step 1: Create the CSS file**

Create `src/pages/FaqPage.module.css` with this exact content:

```css
/* ===== Page shell ===== */
.page {
  min-height: calc(100vh - var(--header-height));
  padding: var(--space-3xl) var(--space-lg) var(--space-6xl);
}

.container {
  max-width: 780px;
  margin: 0 auto;
}

/* ===== Hero ===== */
.hero {
  margin-bottom: var(--space-3xl);
}

.title {
  font-family: var(--font-display);
  font-size: clamp(2rem, 5vw, 3rem);
  font-weight: 600;
  color: var(--color-text);
  margin: 0 0 var(--space-md);
  line-height: 1.15;
}

.subtitle {
  font-size: 1.0625rem;
  color: var(--color-text-secondary);
  margin: 0;
  max-width: 520px;
  line-height: 1.6;
}

/* ===== Category group ===== */
.group {
  margin-bottom: var(--space-3xl);
}

.groupLabel {
  font-size: 0.75rem;
  font-weight: 600;
  letter-spacing: 0.1em;
  text-transform: uppercase;
  color: var(--color-text-muted);
  margin: 0 0 var(--space-md);
}

/* ===== Accordion item ===== */
.item {
  border: 1px solid var(--color-border-visible);
  border-radius: var(--radius-md);
  background: var(--color-surface-raised);
  margin-bottom: var(--space-sm);
  overflow: hidden;
  transition: border-color 0.2s;
}

.item:hover {
  border-color: rgba(255, 255, 255, 0.18);
}

.itemOpen {
  border-color: var(--color-border-glow);
}

.question {
  display: flex;
  align-items: center;
  justify-content: space-between;
  gap: var(--space-md);
  width: 100%;
  background: none;
  border: none;
  cursor: pointer;
  padding: var(--space-md) var(--space-lg);
  text-align: left;
  color: var(--color-text);
  font-family: var(--font-body);
  font-size: 0.9375rem;
  font-weight: 600;
  line-height: 1.4;
  transition: background 0.15s;
}

.itemOpen .question {
  background: var(--color-accent-subtle);
}

.chevron {
  flex-shrink: 0;
  width: 16px;
  height: 16px;
  color: var(--color-text-muted);
  transition: transform 0.25s var(--ease-out-expo), color 0.15s;
}

.itemOpen .chevron {
  transform: rotate(180deg);
  color: var(--color-accent);
}

.answer {
  max-height: 0;
  overflow: hidden;
  transition: max-height 0.35s var(--ease-out-expo);
}

.answerInner {
  padding: 0 var(--space-lg) var(--space-md);
  font-size: 0.9375rem;
  color: var(--color-text-secondary);
  line-height: 1.7;
}

.answerInner a {
  color: var(--color-accent);
  text-decoration: none;
}

.answerInner a:hover {
  text-decoration: underline;
}

/* ===== Footer CTA ===== */
.cta {
  border-top: 1px solid var(--color-border);
  padding-top: var(--space-2xl);
  text-align: center;
  color: var(--color-text-muted);
  font-size: 0.9375rem;
}

.cta a {
  color: var(--color-accent);
  text-decoration: none;
}

.cta a:hover {
  text-decoration: underline;
}
```

- [ ] **Step 2: Commit**

```bash
git add src/pages/FaqPage.module.css
git commit -m "feat: add FaqPage CSS module"
```

---

### Task 2: Create FaqPage component

**Files:**
- Create: `src/pages/FaqPage.tsx`

- [ ] **Step 1: Create the component**

Create `src/pages/FaqPage.tsx` with this exact content:

```tsx
import { useState } from 'react'
import { Link } from 'react-router-dom'
import { usePageTitle } from '../lib/usePageTitle'
import styles from './FaqPage.module.css'

interface FaqItem {
  question: string
  answer: React.ReactNode
}

interface FaqCategory {
  label: string
  items: FaqItem[]
}

const FAQ_DATA: FaqCategory[] = [
  {
    label: 'Getting Started',
    items: [
      {
        question: 'What is OpenThorn?',
        answer:
          'OpenThorn is an AI-powered app builder. You describe what you want to build in plain language, and OpenThorn generates the code and deploys a working website — no coding skills required.',
      },
      {
        question: 'Do I need to know how to code?',
        answer:
          'No. OpenThorn handles the technical side entirely. You write in plain English and the AI takes care of the rest. If you do know how to code, you can still inspect and modify the generated output.',
      },
      {
        question: 'How do I get started?',
        answer: (
          <>
            Sign up for a free account, add an API key for at least one AI provider (such as OpenAI
            or Anthropic), create a project, and start describing your app. The{' '}
            <Link to="/providers">Providers page</Link> shows all supported providers.
          </>
        ),
      },
    ],
  },
  {
    label: 'BYOK & API Keys',
    items: [
      {
        question: 'What does BYOK mean?',
        answer:
          'BYOK stands for Bring Your Own Key. Rather than routing all AI usage through a shared account, you supply your own API key directly from an AI provider. This means you pay your provider directly and OpenThorn never controls your billing.',
      },
      {
        question: 'Where do I get an API key?',
        answer: (
          <>
            From your chosen provider&rsquo;s developer portal — for example,{' '}
            <a href="https://platform.openai.com" target="_blank" rel="noopener noreferrer">
              platform.openai.com
            </a>{' '}
            for OpenAI or{' '}
            <a href="https://console.anthropic.com" target="_blank" rel="noopener noreferrer">
              console.anthropic.com
            </a>{' '}
            for Anthropic. Create an account there, generate an API key, and paste it into
            OpenThorn&rsquo;s Providers page.
          </>
        ),
      },
      {
        question: 'Which AI providers are supported?',
        answer: (
          <>
            OpenAI and Anthropic are supported today, with more on the way. See the{' '}
            <Link to="/providers">Providers page</Link> inside the app for the current full list and
            per-model pricing data.
          </>
        ),
      },
      {
        question: 'Can I use multiple providers?',
        answer:
          'Yes. You can add keys for several providers and choose which model to use on a per-project basis. This lets you balance cost and quality for different types of work.',
      },
    ],
  },
  {
    label: 'Security & Privacy',
    items: [
      {
        question: 'Are my API keys safe?',
        answer:
          'Yes. API keys are encrypted with AES-256-GCM before being stored in our database. They are never logged, never shared with third parties, and are only decrypted at the moment your request is sent to the provider.',
      },
      {
        question: 'What data does OpenThorn store?',
        answer:
          'We store your email address, your encrypted API keys, your project names, and the prompts and generated code within your projects. We do not collect payment information, browsing behaviour, or any data beyond what is needed to provide the service.',
      },
      {
        question: 'Where is my data hosted?',
        answer:
          'Your data is hosted on Vercel's infrastructure. Request data including IP addresses may be processed by Vercel, and data may be transferred to the United States under Standard Contractual Clauses (SCCs) in compliance with GDPR.',
      },
      {
        question: 'Can I delete my data?',
        answer: (
          <>
            Yes. Email{' '}
            <a href="mailto:btalabs.contact@gmail.com">btalabs.contact@gmail.com</a> to request full
            account and data deletion. We will process your request within 30 days.
          </>
        ),
      },
    ],
  },
  {
    label: 'Pricing',
    items: [
      {
        question: 'Is OpenThorn free to use?',
        answer:
          'The OpenThorn platform itself is free. You only pay for the AI usage billed directly by your provider — OpenThorn adds no markup. Your provider charges you based on the model you choose and the number of tokens generated.',
      },
      {
        question: 'Does OpenThorn charge per generation?',
        answer:
          'No. OpenThorn does not take a cut of your AI usage. All generation costs go directly to your provider account, billed according to their standard rates.',
      },
      {
        question: 'Where can I see model pricing?',
        answer: (
          <>
            On the <Link to="/pricing">Pricing page</Link>, which shows live cost and quality data
            for all supported flagship models so you can choose the best model for your budget.
          </>
        ),
      },
    ],
  },
]

function AccordionItem({ question, answer }: FaqItem) {
  const [open, setOpen] = useState(false)

  return (
    <div className={[styles.item, open ? styles.itemOpen : ''].filter(Boolean).join(' ')}>
      <button className={styles.question} onClick={() => setOpen((o) => !o)} aria-expanded={open}>
        <span>{question}</span>
        <svg
          className={styles.chevron}
          viewBox="0 0 16 16"
          fill="none"
          stroke="currentColor"
          strokeWidth="1.75"
          strokeLinecap="round"
          strokeLinejoin="round"
          aria-hidden="true"
        >
          <path d="M3 6l5 5 5-5" />
        </svg>
      </button>
      <div className={styles.answer} style={open ? { maxHeight: '600px' } : undefined}>
        <div className={styles.answerInner}>{answer}</div>
      </div>
    </div>
  )
}

export default function FaqPage() {
  usePageTitle('FAQ')

  return (
    <div className={styles.page}>
      <div className={styles.container}>
        <header className={styles.hero}>
          <h1 className={styles.title}>Frequently Asked Questions</h1>
          <p className={styles.subtitle}>
            Everything you need to know about OpenThorn. Can&rsquo;t find your answer?{' '}
            <a href="mailto:btalabs.contact@gmail.com">Drop us a line.</a>
          </p>
        </header>

        {FAQ_DATA.map((category) => (
          <section key={category.label} className={styles.group}>
            <p className={styles.groupLabel}>{category.label}</p>
            {category.items.map((item) => (
              <AccordionItem key={item.question} {...item} />
            ))}
          </section>
        ))}

        <div className={styles.cta}>
          Still have questions?{' '}
          <a href="mailto:btalabs.contact@gmail.com">Contact us</a> — we&rsquo;re happy to help.
        </div>
      </div>
    </div>
  )
}
```

- [ ] **Step 2: Commit**

```bash
git add src/pages/FaqPage.tsx
git commit -m "feat: add FaqPage component with accordion FAQ"
```

---

### Task 3: Register the route in App.tsx

**Files:**
- Modify: `src/App.tsx`

- [ ] **Step 1: Add the import**

In `src/App.tsx`, add this import after the existing `BlogPostPage` import (around line 21):

```tsx
import FaqPage from './pages/FaqPage'
```

- [ ] **Step 2: Add the route**

In the `<Routes>` block, add the `/faq` route after the `/blog/:slug` route (around line 79):

```tsx
<Route path="/faq" element={<Layout><FaqPage /></Layout>} />
```

- [ ] **Step 3: Verify the file compiles**

```bash
cd "C:/Users/Thoma/OneDrive/Dokumente/Informatik/Bloom"
npx tsc --noEmit
```

Expected: no errors.

- [ ] **Step 4: Commit**

```bash
git add src/App.tsx
git commit -m "feat: register /faq route"
```

---

### Task 4: Wire the footer link

**Files:**
- Modify: `src/components/Footer/Footer.tsx`

- [ ] **Step 1: Replace the placeholder href**

In `src/components/Footer/Footer.tsx`, the `resourcesLinks` array currently has:

```tsx
const resourcesLinks = [
  { label: 'Blog', href: '#' },
  { label: 'Templates', href: '#' },
  { label: 'Guides', href: '#' },
  { label: 'Docs & FAQs', href: '#' },
]
```

Change the "Docs & FAQs" entry to use a `to` property (matching the pattern used by `legalLinks`) and render it with `<Link>` instead of `<a>`:

Replace the entire `resourcesLinks` array and its render block with:

```tsx
const resourcesLinks = [
  { label: 'Blog', href: '#' },
  { label: 'Templates', href: '#' },
  { label: 'Guides', href: '#' },
]

const resourcesRouteLinks = [
  { label: 'Docs & FAQs', to: '/faq' },
]
```

Then in the JSX, find the Resources column (it maps over `resourcesLinks`) and update it to render both lists:

```tsx
{/* Resources */}
<div>
  <div className={styles.colTitle}>Resources</div>
  <div className={styles.colLinks}>
    {resourcesLinks.map((l) => (
      <a key={l.label} href={l.href}>{l.label}</a>
    ))}
    {resourcesRouteLinks.map((l) => (
      <Link key={l.label} to={l.to}>{l.label}</Link>
    ))}
  </div>
</div>
```

- [ ] **Step 2: Verify no TypeScript errors**

```bash
cd "C:/Users/Thoma/OneDrive/Dokumente/Informatik/Bloom"
npx tsc --noEmit
```

Expected: no errors.

- [ ] **Step 3: Commit**

```bash
git add src/components/Footer/Footer.tsx
git commit -m "feat: wire Docs & FAQs footer link to /faq"
```

---

### Task 5: Manual smoke test

- [ ] **Step 1: Start the dev server**

```bash
cd "C:/Users/Thoma/OneDrive/Dokumente/Informatik/Bloom"
npm run dev
```

- [ ] **Step 2: Verify the FAQ page**

Open `http://localhost:5173/faq` and confirm:
- Page title reads "FAQ — OpenThorn"
- All 4 category sections are visible
- Clicking a question expands the answer smoothly
- Clicking again collapses it
- Links inside answers (e.g. to `/pricing`, `/providers`) are clickable

- [ ] **Step 3: Verify the footer link**

Navigate to `http://localhost:5173` (home page), scroll to the footer, and click "Docs & FAQs" — it should navigate to `/faq` without a full page reload.

- [ ] **Step 4: Check page on mobile width**

Resize browser to ~375px wide and confirm the FAQ page is readable and the accordion still works.
